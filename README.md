Rivals skin info thingy

If you have real time protection on malwarebytes, db.bat will break as it did for me, because it executes a powershell command from cmd to an external website so it flags as an exploit payload.. Add it as an exclusion if the issue persists

You can see exactly what is happening here if you're paranoid about viruses
